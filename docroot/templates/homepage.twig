Hello Twig Viewer!
<h1>{{ pageTitle }}</h1>

<div class="row">
    {% for product in products %}
        <div class="span4">
            <h2>{{ product }}</h2>
        </div>
    {% endfor %}
</div>

<div class="row">
    <h3>{{ obj.name }} {{ obj.surname }} is {{ obj.age }}</h3>
    <p>{{ obj.words.0 }} {{ obj.words.1 }} {{ obj.words.2 }}</p>
    <h3>{{ obj.obj1.idea }}</h3>
</div>

{{ dump() }}